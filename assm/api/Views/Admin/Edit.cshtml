@using lab4.Models
@model ApplicationUser

@{
    ViewData["Title"] = "Chỉnh sửa tài khoản";
}

<style>
    .edit-container {
        max-width: 680px;
        margin: 20px auto;
        padding: 36px;
        background: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(12px);
        border-radius: 18px;
        box-shadow: 0 26px 60px rgba(0,0,0,.08);
        border: 1px solid rgba(0,0,0,.06);
    }

    .edit-header {
        display: flex;
        align-items: center;
        gap: 14px;
        margin-bottom: 30px;
    }

    .edit-header h2 {
        font-weight: 700;
        margin: 0;
        font-size: 1.4rem;
    }

    .form-label-custom {
        font-weight: 600;
        font-size: 0.85rem;
        color: #555;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 6px;
    }

    .form-input {
        width: 100%;
        padding: 12px 16px;
        border: 1px solid #e0e0e0;
        border-radius: 10px;
        font-size: 0.95rem;
        transition: all 0.2s;
        background: #fff;
        outline: none;
    }

    .form-input:focus {
        border-color: #000;
        box-shadow: 0 0 0 3px rgba(0,0,0,.06);
    }

    .form-group {
        margin-bottom: 20px;
    }

    .btn-save {
        background: #000;
        color: #fff;
        border: none;
        padding: 12px 32px;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.2s;
    }

    .btn-save:hover {
        background: #333;
        transform: translateY(-1px);
        box-shadow: 0 6px 16px rgba(0,0,0,.15);
    }

    .btn-cancel {
        background: transparent;
        color: #666;
        border: 1px solid #ddd;
        padding: 12px 24px;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.95rem;
        text-decoration: none;
        transition: all 0.2s;
    }

    .btn-cancel:hover {
        background: #f5f5f5;
        color: #333;
    }

    .user-id-hint {
        font-size: 0.8rem;
        color: #999;
        margin-bottom: 20px;
    }
</style>

<div class="edit-container">
    <div class="edit-header">
        <a href="/Admin/Index" class="btn-cancel" style="padding: 8px 14px; font-size: 0.85rem;">← Quay lại</a>
        <h2>✏️ Chỉnh sửa tài khoản</h2>
    </div>

    <div class="user-id-hint">ID: @Model.Id</div>

    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    <form method="post" asp-action="Edit" asp-route-id="@Model.Id">
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label class="form-label-custom">Họ và tên</label>
            <input class="form-input" name="fullName" value="@Model.FullName" placeholder="Nhập họ và tên" />
        </div>

        <div class="form-group">
            <label class="form-label-custom">Email</label>
            <input class="form-input" type="email" name="email" value="@Model.Email" placeholder="Nhập email" required />
        </div>

        <div class="form-group">
            <label class="form-label-custom">Số điện thoại</label>
            <input class="form-input" name="phoneNumber" value="@Model.PhoneNumber" placeholder="Nhập số điện thoại" />
        </div>

        <div class="form-group">
            <label class="form-label-custom">Địa chỉ</label>
            <input class="form-input" name="address" value="@Model.Address" placeholder="Nhập địa chỉ" />
        </div>

        <div class="d-flex gap-3 mt-4">
            <button type="submit" class="btn-save">💾 Lưu thay đổi</button>
            <a href="/Admin/Index" class="btn-cancel">Huỷ bỏ</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
